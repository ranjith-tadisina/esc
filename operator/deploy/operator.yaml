# Generated by IBM TransformationAdvisor
# Wed Sep 16 16:01:43 UTC 2020
apiVersion: apps/v1
kind: Deployment
metadata:
  name: escreening-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: escreening-operator
  template:
    metadata:
      labels:
        name: escreening-operator
    spec:
      serviceAccountName: escreening-operator
      containers:
        - name: escreening-operator
          image: openliberty/operator:0.3.0
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "escreening-operator"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 3
            preference:
              matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
